# Astro build and cache
.astro/
dist/

# Dependencies
node_modules/

# Environment variables
.env
.env.*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch/one-off scripts and exported analytics (not for the public repo)
scratch/

# Superseded design doc, replaced by DESIGN.md
vercel-design-analysis.md.bak

# Everything the test suites generate, in one place — see scripts/testing-paths.mjs.
# The builders, recipes and tests are checked in; what they produce is not,
# because it is all reproducible with `npm run fidelity:fixtures` and a repo
# that ships a megabyte of generated .xlsx is a repo nobody can review.
testing/

# Unrelated nested git repos that live in this folder (never part of the site)
test-repo/
galactic-group/
